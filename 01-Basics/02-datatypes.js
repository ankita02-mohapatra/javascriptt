//basically datatypes are of 2 types 
// Primitive - String , number , bigInt ,  null , undefined , symbol ,boolean.

const score = 100 // number
const isLoggedIn = false // boolean
const repond = null // null
let userEmail; // undefined
var userState = "Odisha" // string
const sujitBankbalance = 343576564546798098765245479886543; // bigInt

const id = Symbol('123') //one character
const anotherId = Symbol('12') //another character


// nonprimitive or reference
//1. array

const sujitCars = ["BMW" , "audi" , "maybeach" , "porche" , "nano"]

//2.function

const myFunc = function (){
    console.log("hello World")
}

//3.object

const myObj = {
    name : "sujit",
    age : 78,
    city : "puducheri"
}